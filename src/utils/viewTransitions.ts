/**
 * View transition types
 *
 * These are used to define the type of view transition to use.
 * You can find the CSS for these in the app.css file.
 */
export enum ViewTransitionType {
  SlideUp = "slide-up",
  SlideDown = "slide-down",
  SlideLeft = "slide-left",
  SlideRight = "slide-right",
  Flip = "flip",
}
